#include "Appointment.hpp"

void Appointment::deleteEntry(std::string searchTerm, int searchType) {
	
	if (numberOfAppointments <= 0) {
		cout << "cannot delete entry if no appointments";
		return;
	}
	// delete operations


};
